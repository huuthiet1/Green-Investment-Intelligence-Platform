{
  "name": "green-investment-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Green Investment Intelligence Platform",

  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },

  "repository": {
    "type": "git",
    "url": "https://github.com/huuthiet1/Green-Investment-Intelligence-Platform.git"
  },

  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
  "@vitejs/plugin-react": "latest",
  "vite": "^8.0.8"
},

  "license": "ISC"
}